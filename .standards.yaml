# Anti-rot exemption ledger (nicerobot/tools.build/standards/capabilities.yaml).
# Exemptions must shrink toward zero.
exempt:
  gate:lint: "yze conformance backlog — 84 findings remaining (2026-07-17: the library core moved to tsvsheet/go-isnow born fully conformant, taking its 130 findings with it; what remains is CLI-internal API shape: ~33 namedtypes, ~20 ptrparam, ~19 ptrrecv, ~9 boolname, + valuector/globalvar/errconst singletons); task 14 in the agent queue owns the burn-down. The ratchet hard-fails this entry the moment lint passes."
